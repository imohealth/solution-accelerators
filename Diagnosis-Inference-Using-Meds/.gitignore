# Local environment — contains AWS profile name, never commit
.env

# Config with credentials — never commit (use config.json.template)
config.json

# Virtual environment
.venv/
venv/
env/

# Python cache
__pycache__/
*.pyc
*.pyo

# Jupyter checkpoints
.ipynb_checkpoints/

# Claude Code session config
.claude/
