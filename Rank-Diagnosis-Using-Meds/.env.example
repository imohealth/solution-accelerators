# Copy this file to .env and fill in your values.

# IMO Normalize API credentials (OAuth client credentials grant)
IMO_NORMALIZE_CLIENT_ID=
IMO_NORMALIZE_SECRET=

# IMO Knowledge Graph (GOAT) API credentials
IMO_KG_CLIENT_ID=
IMO_KG_CLIENT_SECRET=

# Optional: named AWS CLI profile that has access to SSM parameters.
# Leave empty on SageMaker / EC2 / ECS — boto3 uses the instance role automatically.
